# Development + CI tools (testing, linting). Not installed in the production image.
# Includes the training deps because the test suite exercises the train workflow.
-r requirements-train.txt
pytest>=8.0
httpx>=0.27
ruff>=0.5
