# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# MLflow local tracking store (regenerated by training; can get large)
mlruns/
mlartifacts/
mlflow.db

# Raw data — downloaded on demand by src/churn/data.py, no need to commit.
data/raw/*.csv

# OS noise
.DS_Store

# NOTE: we intentionally DO commit models/*.joblib and the drift reference so the
# Docker image and free deploy work without a training run. They're small.
