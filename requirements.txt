# Runtime dependencies (what the deployed API needs to run).
# Kept separate from dev tools so the Docker image stays small.
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
optuna>=3.6
mlflow>=2.14
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
joblib>=1.4
scipy>=1.11
requests>=2.31
